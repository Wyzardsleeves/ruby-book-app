## Instructions

#### Secripts

Bundle install
run `$ bundle install`

or

If 'bundle install' doesn't work.
run `gem install 'httparty'`
run `gem install 'colorize'`

Then start the app:
run `$ ruby books.rb`
